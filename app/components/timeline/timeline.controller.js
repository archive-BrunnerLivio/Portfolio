(function () {
    'use strict';

    function TimelineController() {
    }
    /**
     * @ngdoc controller
     * @name portfolio.timeline.directive:TimelineController
     * @description
     * Controller for the timeline-directive
     */
    angular
        .module('portfolio.timeline')
        .controller('TimelineController', TimelineController);
})();